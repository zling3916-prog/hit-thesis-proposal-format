---
name: hit-thesis-proposal-format
description: 把哈尔滨工业大学（HIT）硕士学位开题报告草稿一键调整为符合《硕士学位论文报告模板（2025）》的规范格式：页面设置（A4/页边距/去页眉）、字体（正文宋体、标题黑体、数字英文 Times New Roman）、层级标题（节/条/款/项的字号与段前段后）、固定 21pt 行距与首行缩进、参考文献条目格式，并可重建模板封面。当用户提供开题报告草稿并要求"按模板调整/统一格式/套格式/排版"，或要求生成符合哈工大 2025 开题报告模板格式的文档时使用。
---

# HIT 硕士学位开题报告格式调整

## 何时使用

- 用户给出开题报告（或论文报告）草稿 docx，要求按哈工大 2025 开题报告模板统一格式。
- 用户要求把内容（大纲、正文、已有报告）排成符合模板版式的开题报告。
- 需要重建模板封面（题目/学院/学科/导师/研究生/学号/日期）。

## 格式规范

完整规范见 [`references/format-spec.md`](references/format-spec.md)。核心参数：

| 项 | 值 |
|---|---|
| 页面 | A4，上2.5 下2.2 左2.5 右2.5cm，无页眉 |
| 正文 | 宋体 小四(12pt)，数字/英文 Times New Roman，固定行距 21pt，段前/段后0，首行缩进2字符 |
| 节标题(1) | 黑体 小3号(15pt)，段前/段后0.5行，顶格 |
| 条标题(1.1) | 黑体 4号(14pt)，段前/段后0.5行，顶格 |
| 款标题(1.1.1) | 黑体 小4号(12pt)，段前/段后0，顶格 |
| 项（（1）…） | **按正文处理**（宋体小四 12pt，首行缩进2字符），不作标题 |
| 参考文献 | 宋体/TNR 12pt，固定21pt，**悬挂缩进** |
| 封面居中行 | 加粗、居中、**无缩进** |
| 封面填表行 | 标签无下划线，填充区（值）下划线，用制表位对齐 |

## 使用流程

1. **确认输入与输出**：拿到草稿 docx 路径与目标输出路径。可先复制一份草稿，在副本上操作。
2. **（可选）准备封面元数据**：若需重建封面，准备 JSON：`{"title":"...","degree":"学位论文|实践成果","college":"...","discipline":"...","advisor":"...","student":"...","student_id":"...","date":"..."}`。题目/姓名等无法从上下文确定时用占位符，不要编造。
3. **运行脚本**：
   ```bash
   python scripts/format_proposal.py <输入.docx> <输出.docx> [--cover-json 封面.json]
   ```
   若草稿已自带封面，脚本会自动识别并套用封面格式，无需 --cover-json。
4. **校验产物**（必须做，不能用"看起来没问题"代替）：
   - 用 `python-docx` 回读输出：抽查若干段落的 `w:rFonts`（宋体/黑体/Times New Roman）、`w:sz`（15/14/12pt）、`w:spacing`（line=420, lineRule=exact）、`w:ind`（正文 firstLineChars=200，标题顶格，参考文献 left=480+hanging=480，封面填表行 left=1260+制表位4500）与 `w:snapToGrid=0`。
   - 用 LibreOffice 渲染为 PDF 或逐页查看，检查：封面是否正常（居中行无缩进、填表行下划线对齐）、标题层级是否分明、正文是否缩进统一、参考文献是否为悬挂缩进、页眉是否为空。
   - 核对正文文字未被改动（格式脚本只改格式不动文字，比对文本即可确认）。
5. **交付**：把输出 docx 路径交给用户，说明已应用的格式项与验证结果。

## 处理规则要点

- **层级识别**：优先使用 Word 内置 Heading 1/2/3 样式；无样式时按编号正则识别（`1．`/`1.`/`1、`→节，`1.1`→条，`1.1.1`→款）。`（1）（2）…` 开头的条目**按正文处理**（宋体、首行缩进2字符=题序空4个半角字符），不作标题。
- **参考文献区**：从标题含"参考文献"的节开始，到下一个节标题前，条目采用**悬挂缩进**（首行顶格、续行缩进2字符）。
- **封面**：仅对文档前 30 段中匹配封面字段的行套封面格式；居中行（校名/标题/学位类型/研究生院制）清除缩进；填表行标签不加下划线、填充区（值）加下划线并用制表位对齐，保证下划线对齐。
- **表格与图片**：脚本不改动表格与图片内容；页面设置对全文生效。
- **空段**：保持空白，不套正文格式。

## 已知边界

- 行距固定 21pt 为模板建议（每页约 33 行）；如用户要求其他行距，修改脚本常量 `LINE_TWIPS` 后重跑。
- 若输入文档自带复杂页眉页脚，脚本只清空页眉文字，不删除页眉链接关系。
- 封面重建以模板封面为准；用户提供的封面如果字段缺失，用占位符补齐并提示。
